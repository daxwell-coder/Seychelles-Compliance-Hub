name: Placeholder (Disabled)
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Secondary ci.yaml placeholder; main workflow is ci.yml"
      - run: echo "Secondary ci.yaml placeholder; main workflow is ci.yml"

